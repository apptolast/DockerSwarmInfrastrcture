---
image_preflight_expected_os: linux
image_preflight_expected_architecture: amd64
# Stacks whose images are cached before any stack mutation. OrganizationWeb
# verifies its own images after deploy and is deliberately not listed.
image_preflight_stacks:
  - edge
  - observability
  - workloads
# Stacks whose channel entries are resolved against the registry in this
# operation. Holds are always pulled by digest; channels only when listed.
image_preflight_channel_stacks: []
