---
- name: Restart restricted Docker API proxy
  ansible.builtin.systemd_service:
    name: "{{ observability_docker_proxy_service }}"
    daemon_reload: true
    enabled: true
    state: restarted
  listen: Restart restricted Docker API proxy
