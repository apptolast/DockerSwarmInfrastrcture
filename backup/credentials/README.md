# No guardar credenciales aquí

Este directorio solo existe como defensa adicional contra una copia
accidental. Las credenciales reales pertenecen exclusivamente a
`/etc/dockerswarm/backup`, fuera del repositorio, como ficheros
`root:root 0600`.
